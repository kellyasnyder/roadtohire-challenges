import React from 'react';
import App from './app';

const Header = (props) => {
    <div>
        <h1>Start Game</h1>
        <Button onClick={this.startStandard}>Standard</Button>
        <Button onClick={this.startExpert}>Expert</Button>
    </div>
}


export default Header;